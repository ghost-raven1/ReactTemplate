import styled from "styled-components";
import {Card, Typography} from "antd";

export const CardStyled = styled(Card)`
    max-width: 400px;
    margin: 50px auto 0;
`;

export const CardTitleStyled = styled(Typography.Title)`
    text-align: center;
    margin-bottom: 24px;
`;
